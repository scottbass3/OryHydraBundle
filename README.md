# Harbor API Bundle

A Symfony bundle to communicate with Ory Hydra API. This bundle is a wrapper for [ory hydra client](github.com/ory/hydra-client-php).

## Installation
If your application is using Symfony Flex, run this command to install the bundle :
```shell
composer require scottbass3/ory-hydra-bundle
```
